# OpenCode → CodeBrain

This project’s agent contract lives at the repo root:

→ **[../AGENTS.md](../AGENTS.md)**

Always:

1. `./scripts/agent/preflight.sh` before coding
2. `./scripts/agent/postcheck.sh` after coding
3. Commits: Conventional Commits, **no** `Co-Authored-By` / AI trailers
